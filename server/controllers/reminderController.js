export async function createReminder(req, res) {
  res.status(501).json({ message: "Create reminder endpoint scaffolded." });
}

export async function getReminders(req, res) {
  res.status(501).json({ message: "List reminders endpoint scaffolded." });
}
